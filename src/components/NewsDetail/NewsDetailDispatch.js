// import {LOAD_NEWS_LIST} from "../../reducers/ReducerNews";

export default function NewsDetailDispatch(dispatch) {
	return {
		// onLoadNewsList: () => {
		// 	const asyncLoadNewsList = () => {
		// 		return dispatch => {
		// 			setTimeout(() => {
		// 				console.log('asyncLoadNewsList');
		// 				dispatch({type: LOAD_NEWS_LIST});
		// 			}, 1000)
		// 		}
		// 	}
		// }
	};
}
